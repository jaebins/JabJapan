export default function Practice_foot(){
    return(
        <div className="practice-foot">
            <a id="practice-foot-wordsMap" onClick={() => window.open("/Dictionary_Word", "dicionary", "width=600; height=600")}>Dictionary</a>
        </div>
    )
}